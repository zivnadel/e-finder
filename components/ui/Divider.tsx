import React from "react";

// sections divider component

const Divider: React.FC = () => {
  return (
    <div className="bg-gradient-to-r from-primary to-secondary w-full h-[3px] my-5 rounded-full opacity-60"></div>
  );
};

export default Divider;
